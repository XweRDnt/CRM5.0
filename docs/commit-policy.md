# Commit Policy

- 1 method = 1 atomic commit.
- Commit message template:
  - `feat(service): implement <Service.method>`
  - `test(service): add tests for <Service.method>`
- No mixed commits for unrelated services.
- No commit without green `lint` and `test`.

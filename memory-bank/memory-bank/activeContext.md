# VideoFeedback — Active Context

> Обновляй этот файл после каждой сессии разработки.

## Текущий фокус

Улучшение продукта на основе обратной связи + разработка ключевых фич для ICP.

### Контекст сессии 2026-03-28

- Проведен полный аудит пользовательской производительности и серверной части.
- Подтверждено, что основной bottleneck был не в железе VPS, а в client-heavy user flow, лишних roundtrip'ах и тяжелой странице версии.
- Для пользовательского контура переведены на server-first / облегчены:
  - `/projects`
  - `/team`
  - `/scope`
  - `/settings`
  - вход в `/projects/[id]`
  - вход в `/projects/[id]/versions`
- Добавлен dashboard user context, чтобы убрать часть повторных auth-check на внутренних user-экранах.
- Усилен health-check: теперь проверяет `database` и `redis`.
- Ключевые user API получили `Server-Timing`.
- Deploy flow усилен:
  - `npm ci`
  - очистка `.next`
  - использование локального Prisma CLI из `node_modules/.bin`
  - health-check по `/api/health`
- Production build переведен на `next build --webpack`, потому что turbopack build на VPS давал нестабильные артефакты и 502.
- Во время этой сессии уже был реальный продовый инцидент `502 Bad Gateway`, успешно восстановлен.
- Исправлена битая кириллица на user-страницах проектов после server-first рефактора.
- Исправлен мобильный баг dashboard sidebar внутри страницы версии проекта: мобильный drawer теперь рендерится через portal в `document.body`, чтобы не зависеть от stacking context тяжелого project/version UI.
- Полностью убран special-case sidebar flow: dashboard переведен на единый глобальный `DashboardShell`, общий header больше не скрывается на странице версии проекта, а сама страница версии больше не управляет sidebar локально.
- После первого push найден и исправлен остаточный баг нового global sidebar: mobile portal sidebar должен жить под `.dashboard-shell`, иначе portal теряет glass/dashboard-стили.
- Старый `Sidebar.tsx` и `MobileMenuButton.tsx` удалены; вместо них собран новый `GlobalSidebar` и `SidebarToggleButton` с отдельным контрактом на desktop/mobile поведение.
- Текущий следующий приоритет: облегчить страницу версии проекта, так как она остается самым тяжелым маршрутом и вероятным источником мобильных микро-фризов.

## Критические фичи (блокеры для правильного ICP)

- [x] Аннотации на кадре (рисование) — блокер
- [ ] Демо-аккаунт (read-only ссылка, IS_DEMO флаг) — нужен для холодного outreach
- [x] Public demo-вход во внутренний workspace без логина: только Projects + Version review, read-only

## В работе — Приоритет 1 (базовое удобство)

- [x] Удаление проектов с очисткой Kinescope-видео
- [x] Удаление версий
- [x] Кнопка открытия ссылки гостевого портала (не копировать, а открывать)
- [x] Сброс и пересоздание ссылки гостевого доступа

## Приоритет 2 — Аналитика

- [ ] Дашборд использования для владельца: Гб хранилища, минуты транскодинга, лимиты по тарифу
- [ ] Исправить баг: trafficGb всегда 0 в KinescopeUsageSnapshot
- [ ] Исправить баг: сломан rollover подписки

## Приоритет 3 — Команда

- [ ] Роль PM: назначение монтажёров, управление правами комментирования
- [ ] Улучшить UX добавления сотрудников

## Приоритет 4 — Клиентский портал (Frame.io стиль)

- [ ] Первый вход в портал: модалка с вводом имени до открытия просмотра видео и комментирования
- [ ] Голосовые комментарии
- [ ] Прикрепление файлов к комментариям
- [x] Треды/обсуждение внутри каждой правки
- [ ] Общий чат монтажёр ↔ клиент вне контекста конкретной правки
- [ ] Задачи из правок (комментарий → задача с исполнителем / рабочую единицу команды)
- [ ] Статусы для клиента

## Приоритет 5 — Уведомления

- [ ] Telegram бот для сотрудников агентства
- [ ] Telegram бот для клиентов (статус работ, уведомление "взяли в работу", готова новая версия)

## Недавно сделано

- Проведен holistic perf/stability pass по user-части без захода в admin-маршруты
- Выполнен prod recovery после 502, связанного с server-first scope mapping и нестабильной turbopack build-схемой
- Переведен production build на webpack path для стабильных выкладок
- Исправлена кодировка UI-копира на страницах проектов
- Исправлен mobile sidebar внутри project version page через portal-рендер мобильного drawer
- Весь dashboard sidebar flow унифицирован через один глобальный shell без route-specific исключений
- Реализован public internal workspace demo без логина: `/workspace-demo/[token]` с read-only экранами Projects + Version review
- Реализован сброс ссылки гостевого портала из рабочего интерфейса версии
- Реализованы треды/обсуждения по каждой правке (feedback threads + read state)
- Есть отдельная страница Scope Guard и backend-контур решений, но модуль пока не является активным продуктовым приоритетом
- Удалён legacy `ios` theme shell, который смешивал светлую тему с dashboard glass UI
- Притемнён sidebar в dashboard: убрана белёсая подложка у пунктов "Проекты / Клиенты / Команда"
- Реализована система биллинга (4 тира FREE/START/GROWTH/BUSINESS)
- Роли OWNER / EDITOR
- Версионирование с модальным окном загрузки
- Admin-панель для ручного переключения тарифов
- Invite-ссылки для добавления сотрудников
- Аннотации на кадре: инструменты рисования, превью, просмотр в портале и дашборде
- Мобильный режим аннотаций в клиентском портале (псевдо-fullscreen)
- Исправлены баги мобильного fullscreen (fallback фон, клики по виджету)
- Постер из Kinescope API для mobile annotation через серверный endpoint
- Отрисовка постера с object-contain на мобилке
- Мобильные аннотации переведены на единый оверлей поверх живого плеера
- Добавлен touch-action для оверлея, чтобы ловить тач-события
- Удалены non-Kinescope провайдеры (schema, API, сервисы, UI, тесты)
- Плавающий мобильный тулбар аннотаций поверх плеера (bottom-center)
- Отправка аннотаций без обязательного текстового комментария
- Аннотации в портале показываются только на паузе и исчезают при воспроизведении
- Сохранение имени автора в портале (localStorage) + подсказка "комментарий необязателен" при аннотации
- Убрано авто-показ по таймкоду: аннотации показываются только по клику на таймкод и скрываются при play
- Убран авто-фокус на комментарии во время рисования (мобильный скролл больше не прыгает)
- Псевдо-fullscreen с кадром по таймкоду не реализован: Kinescope даёт только iframe/preview, нет прямого медиа-URL; остаёмся с overlay поверх плеера
- Удалена генерация/загрузка превью аннотаций (S3 preview endpoint выключен)
